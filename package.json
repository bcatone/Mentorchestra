{
  "name": "mentorchestra",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "api": "cd backend && rails s",
    "app": "npm start --prefix frontend",
    "all": "npm-run-all --parallel api app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
